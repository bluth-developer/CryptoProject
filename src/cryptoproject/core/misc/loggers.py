import logging

credentials = logging.getLogger("core.credentials")
contracts = logging.getLogger("core.contracts")
cex = logging.getLogger("core.cex")
dex = logging.getLogger("core.dex")
telegram = logging.getLogger("telegram.logger")